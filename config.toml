# The URL the site will be built for
base_url = "https://everettsprojects.com"
title = "Everett's Projects"
description = "A place to write about and share anything I find interesting"


# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "zola-folio"

taxonomies = [
    {name = "tags"}
]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true


[extra]
math = true

# Put all your custom variables here
menu_items = [
    {name = "about", url = "/pages/about"},
    {name = "projects", url = "/pages/projects"},
    {name = "photography", url = "/pages/photography"},
]

author = "Everett Robinson"
show_only_description = true
